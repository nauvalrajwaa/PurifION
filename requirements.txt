# Python dependencies
# Requires Python >= 3.8
pysam>=0.22.0,<0.24.0

# System dependencies (not pip-installable — install via conda or system package manager)
# minimap2 >= 2.26
# samtools >= 1.17
# flye >= 2.9 (only needed if --run-assembler is used)
